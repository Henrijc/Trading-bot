FROM python:3.11-slim

WORKDIR /freqtrade

# Install all build dependencies, compile TA-Lib, UPDATE LIBRARY CACHE, and then clean up
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        build-essential \
        wget && \
    wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz -q -O ta-lib-0.4.0-src.tar.gz && \
    tar -xzvf ta-lib-0.4.0-src.tar.gz && \
    cd ta-lib && \
    ./configure --prefix=/usr && \
    make && \
    make install && \
    # This is the critical command to update the library cache
    ldconfig && \
    cd .. && \
    # Clean up build artifacts and package lists
    rm -rf ta-lib ta-lib-0.4.0-src.tar.gz && \
    apt-get purge -y --auto-remove build-essential wget && \
    rm -rf /var/lib/apt/lists/*

# Copy requirements first for better caching
COPY freqtrade/requirements.txt .

# Upgrade pip and install requirements
RUN pip install --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt

# Copy application code
COPY freqtrade/ .

# Create necessary directories
RUN mkdir -p /freqtrade/user_data/models && \
    mkdir -p /freqtrade/user_data/logs && \
    mkdir -p /freqtrade/logs

# Create non-root user
RUN useradd -r -s /bin/false frequser && \
    chown -R frequser:frequser /freqtrade

# Expose port
EXPOSE 8082

# Switch to non-root user
USER frequser

# Default command
CMD ["python3", "luno_trading_bot.py", "--config", "config.json", "--strategy", "LunoFreqAIStrategy"]