name: Deploy to VPS

on:
  push:
    branches: [ for-deployment ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to VPS
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        key: ${{ secrets.VPS_SSH_KEY }}
        script: |
          cd /opt/crypto-coach
          git fetch --all
          git reset --hard origin/for-deployment
          cd vps_deployment_package/docker
          sudo docker-compose --env-file /opt/crypto-coach/.env -f docker-compose.prod.yml up -d --build --remove-orphans